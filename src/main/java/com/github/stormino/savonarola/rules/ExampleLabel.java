package com.github.stormino.savonarola.rules;

public enum ExampleLabel {
    /** The message violates the rule. */
    POSITIVE,
    /** The message looks close to the line but does NOT violate the rule. */
    NEGATIVE
}
